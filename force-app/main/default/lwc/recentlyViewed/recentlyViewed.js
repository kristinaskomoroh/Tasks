import { LightningElement } from 'lwc';
import RecentlyViewedLabel from '@salesforce/label/c.RecentlyViewedLabel';

export default class RecentlyViewed extends LightningElement {

    labels = {
        RecentlyViewedLabel
    };
}